package io.hops;

/**
 * Exists to be used as a wrapper around ClusterJ's EventOperation,
 * which itself is a wrapper around NDB's EventOperation class.
 */
public class HopsEventOperation {

    public HopsEventOperation() {

    }
}
